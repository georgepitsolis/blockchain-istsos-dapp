<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title><%= title %></title>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
</head>

<body>

    <div class="container py-4">
        <header class="pb-3 mb-4 border-bottom">
            <a href="/" class="d-flex align-items-center text-dark text-decoration-none">
                <span class="fs-4">Dapp for istSOS</span>
            </a>
        </header>
    
        <div class="row align-items-md-stretch">
            <div class="col-md-6">

                <div class="col align-items-md-stretch">

                    <div class="p-5 mb-4 text-white bg-dark rounded-3">
                        <h5>Connect to istSOS database</h5>
                        <div class="input-group input-group-sm mb-3">
                            <span class="input-group-text" id="inputGroup-sizing-default">Url</span>
                            <input type="text" id="url-input" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">
                        </div>
                        <div class="input-group input-group-sm mb-3">
                            <span class="input-group-text" id="inputGroup-sizing-default">Db</span>
                            <input type="text" id="db-input" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">
                        </div>
                    </div>

                    <div class="p-5 mb-4 text-white bg-dark rounded-3">
                        <div class="table-responsive-sm">
                            <table class="table table-sm table-dark table-hover">
                                <thead>
                                    <tr>
                                        <th>Station name</th>
                                        <th>File name</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody id="table-selected-files">
                                </tbody>
                            </table> 
                        </div>

                        <button class="btn mt-4 btn-light" type="button" id="btm-select-files">Select files</button>
                        <input type="file" id="selected-files" name="files" accept=".txt" style="display: none;" multiple>
                    </div>

                </div>

            </div>

            <div class="col-md-6">
                <div class="h-100 p-5 bg-light rounded-3">
                    <h2>Blockchain history</h2>
                    <p>All previous additions which have been applied on blockchain</p>
                    <a class="btn btn-outline-dark" type="button" id="btm-timeline" href="/timeline"><i class="bi bi-clock-history"></i> Timeline</a>
                </div>                
            </div>
        </div>
    
        <footer class="pt-3 mt-4 text-muted border-top">
            Copyright &copy; 2022 Giorgos Pitsolis ECE NTUA
        </footer>

        <div id="liveAlertPlaceholder"></div>
    </div>

    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    
    <script>
        $(document).ready(function() {
            
            var allfiles = []

            // Remove a row from table
            $('#table-selected-files').on('click', '.remove', function () {
                $(this).closest('tr').remove();
            });

            // Select 
            $("#btm-select-files").on('click', function() {
                $('#selected-files').trigger('click');
            });

            $("#selected-files").change(function() {
                
                for (var i = 0; i < $(this).get(0).files.length; ++i) {
                    if (allfiles.includes($(this).get(0).files[i].name) == false) {
                        allfiles.push($(this).get(0).files[i].name);

                        // Adding a row inside the tbody.
                        $('#table-selected-files').append(
                            `<tr>
                            <td>${(allfiles[i]).split('_')[0].toUpperCase()}</td>
                            <td>${allfiles[i]}</td>
                            <td>
                                <button style="all: unset; cursor: pointer;" type="button" class="btn remove"><i class="bi bi-trash"></i></button>
                            </td>
                            </tr>`);
                    }
                }
                
                console.log(allfiles);
            });            
        });

    </script>
</body>

</html>